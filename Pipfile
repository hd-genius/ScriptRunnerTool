[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[dev-packages]
pyfakefs = "*"
parameterized = "*"

[scripts]
test = "python -m unittest discover -s src -p *_test.py"

[requires]
python_version = "3.11"
